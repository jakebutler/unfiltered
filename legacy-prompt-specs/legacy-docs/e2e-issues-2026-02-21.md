# E2E Issue Log — 2026-02-21

## Scope
Attempted to run full browser E2E from terminal automation and validate local dev startup for:
- study list/create
- participant join/interview
- findings dashboard

## Findings

1. `P0` Convex generated client files missing initially
- Symptom: Next.js compile failed on `@/convex/_generated/api` not found.
- Repro: `curl http://localhost:3000/studies` (when dev server runs) returned module-not-found error page.
- Status: Mitigated with temporary fallback files:
  - `convex/_generated/api.ts`
  - `convex/_generated/dataModel.ts`
  - `convex/_generated/server.ts`
- Note: These are temporary shims and should be replaced by real Convex codegen output.

2. `P0` Convex CLI unavailable in this sandbox environment
- Symptom: `npx convex dev` and `npx convex codegen` fail before setup completes.
- Errors observed:
  - `TypeError: fetch failed`
  - `getaddrinfo ENOTFOUND o1192621.ingest.sentry.io`
- Impact: Could not perform real Convex project bootstrap/codegen from this environment.

3. `P0` Playwright CLI unavailable in this sandbox environment
- Symptom: wrapper command could not fetch `@playwright/mcp`.
- Error observed:
  - `npm error code ENOTFOUND`
  - `request to https://registry.npmjs.org/@playwright%2fmcp failed`
- Impact: Could not run automated browser E2E from this environment.

4. `P1` Next.js turbopack workspace-root warning
- Symptom: dev server warns it selected `/Users/jacobbutler` due multiple lockfiles.
- Impact: Warning only, but increases risk of path-resolution confusion.

## What Is Ready Now

- App compiles with temporary Convex generated-file shims.
- Unit tests pass: `7` test files, `38` tests.
- Documentation reflects canonical route/model decisions.

## Manual Test Checklist (Run On Your Machine)

1. Run Convex setup/codegen in repo root:
```bash
npx convex dev
```

2. Start web app:
```bash
npm run dev
```

3. Run E2E flow manually:
- Open `/studies/new`, create a study (Mode `B`).
- Open `/join/[studyId]`, grant mic/camera as desired, start session.
- Complete at least one task and end session.
- Verify redirect to `/dashboard/[sessionId]`.
- Verify themes, moment cards, heatmap tab, exports.

4. After successful Convex codegen:
- Remove temporary fallback files in `convex/_generated/` if overwritten by Convex.
